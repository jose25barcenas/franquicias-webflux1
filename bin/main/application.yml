spring:
  application:
    name: franquicias-webflux
  r2dbc:
    url: r2dbc:postgresql://postgres:5432/franquicias
    username: postgres
    password: postgres
    pool:
      initial-size: 5
      max-size: 20

server:
  port: 8080

logging:
  level:
    com.franquicias: DEBUG
